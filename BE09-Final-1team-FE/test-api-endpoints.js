// API 엔드포인트 테스트를 위한 유틸리티 파일
// 이 파일은 CSS에서 참조되어 빌드 오류를 방지하기 위해 생성되었습니다.

export const testApiEndpoints = {
  // API 엔드포인트 테스트 함수들
  testNewsletterEndpoints: async () => {
    console.log('뉴스레터 API 엔드포인트 테스트');
  },
  
  testUserEndpoints: async () => {
    console.log('사용자 API 엔드포인트 테스트');
  },
  
  testNewsEndpoints: async () => {
    console.log('뉴스 API 엔드포인트 테스트');
  }
};

export default testApiEndpoints;
